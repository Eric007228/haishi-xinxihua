# 技术设计文档（TECH_DESIGN）——海事信息化课程笔记网站

| 项目 | 内容 |
|---|---|
| 文档版本 | v2.0（v1.0 通过 Day 5 自检后，按完整模板扩充：方案四维对比、项目结构、数据对象、API 清单、错误处理、环境变量、部署与迁移） |
| 撰写日期 | 2026-09-20（Day 5） |
| 作者 | Eric007228 |
| 上游文档 | 《PRD.md》v1.1（Day 4 产出）——本文档回答 PRD 交接来的两个问题：托管平台选哪家、Markdown 渲染怎么做 |
| 我的限制 | 零基础起步、业余时间少、只有 GitHub 一个账号、Windows 电脑、无服务器预算——**选型全部围绕这些限制做** |
| 今日边界 | 只定"用什么做"和"数据怎么流"，不写一行代码；不回头改 PRD |

---

## 0. 一句话数据流（今天的主线）

> **数据从"我电脑里的 Markdown 笔记"来，经 git push 进入 GitHub 仓库（它就是本项目的数据库），由 GitHub Pages 分发到访客浏览器，浏览器里的 docsify 把 Markdown 渲染成网页并完成本地搜索，最终显示在屏幕上。**

## 1. 前后端与数据库分工（先搞清分工，再选工具）

用餐厅打比方：

| 角色 | 餐厅里的对应 | 干什么活 |
|---|---|---|
| **前端**（Frontend） | 大厅 + 菜单 + 服务员上菜 | 用户看得见、点得动的部分：页面长什么样、按钮点了有什么反应 |
| **后端**（Backend） | 厨房 | 幕后处理：收到点单后计算、加工、查库存 |
| **数据库**（Database） | 仓库 | 长期存放原料（数据），厨房需要时去取 |

**本项目的关键判断：我们是"纯静态网站"，后端和数据库这两个角色都不需要请人。**

- 没有"厨房"：访客不需要登录、不需要提交订单，浏览器自己就能把菜端上桌——不存在要在服务器上实时计算的事
- 没有"仓库"：内容就是 Markdown 文件本身，**GitHub 仓库直接兼任数据库**——PRD 第 5 节的元数据（title / chapter / category / file / updated）就放在笔记文件和目录配置里，不用额外建库
- "厨房"剩下的活（把 Markdown 变成好看的文章、做关键词搜索）全部交给**访客浏览器里的 JS** 完成

这个判断不是偷懒，是省下整个后端开发 + 服务器成本，而 PRD 的 4 项功能一个都不缺。

## 2. 技术路线：方案 A / 方案 B 对比与推荐

**方案 A：GitHub Pages + docsify（无构建、运行时渲染）**
**方案 B：Vercel + MkDocs（本地构建、生成纯 HTML 再发布）**

按四个维度逐项比较（结合"我的限制"打分）：

| 维度 | 方案 A（GitHub Pages + docsify） | 方案 B（Vercel + MkDocs） | 谁占优 |
|---|---|---|---|
| **学习成本** | 只需学一个 `index.html` 配置块；但要理解"浏览器现场渲染"这个新概念 | 要装 Python 环境、学 MkDocs 配置和构建命令，还要理解"构建产物"概念，链条长一截 | **A**（零基础 + 业余时间少） |
| **线上持久化** | 站点跟着 GitHub 仓库走：push 即持久，只要仓库在网站就在；无额度焦虑 | 站点跟 Vercel 账号走：免费版有带宽额度（约 100GB/月，本项目远用不到）；数据源仍在 GitHub 仓库 | 打平（本项目量级下都够） |
| **费用** | 全程 0 元，不需要任何新账号 | 免费额度内也是 0 元，但要**新注册 Vercel 账号**并授权仓库——对"只有 GitHub 一个账号"的我是一条新维护线 | **A**（账号限制） |
| **排错难度** | 出错点集中：配置全在 `index.html` 一处；但报错藏在浏览器控制台，新手要学着按 F12 看 | 本地构建报错信息清晰，但"本地能建 ≠ 线上能建"，多一道工序就多一类线上/本地不一致的坑 | **A**（出错面更小） |

**推荐：方案 A。** 理由绑死我的三条限制：① 业余时间少 → 学习链条越短越好，A 不用装 Python、不用学构建；② 只有 GitHub 账号 → A 零新账号，B 要多维护一条授权关系；③ 排错能力有限 → A 的出错面集中在一个文件里。

**保留的退路：** 若上线实测 GitHub Pages 在常用网络环境打不开（Day 5 已知国内访问 github.io 偶尔不稳），按第 10 节迁移清单切方案 B，仓库内容不用动。

### 2.1 备选方案的存档（为什么不考虑其他）

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| 云服务器 + 自建站 | 完全可控 | 要花钱、要运维，对纯静态站纯属浪费 | 不考虑 |
| 纯手写 HTML | 学习价值最高 | 目录、搜索全要自己造轮子，MVP 周期内做不完 | 不采用 |

## 3. 项目结构（规划稿，Day 6 动手建）

```
海事信息化/
├── index.html        # 网站入口：引入 docsify + 全部配置（站点名、搜索、侧边栏开关）
├── .nojekyll         # 空文件：让 GitHub Pages 原样发布 _ 开头的文件（没有它 _sidebar.md 会被忽略！）
├── _sidebar.md       # 目录文件：title/chapter/category 元数据写在这里
├── _404.md           # 自定义"页面不存在"提示页
├── README.md         # 打开网站根路径时显示的首页说明
├── notes/            # 笔记正文目录（唯一的内容数据源）
│   ├── chapter-1/
│   │   └── port-info-system.md   # 示例：第 1 章某篇笔记
│   └── chapter-2/
│       └── modern-it.md
├── .gitignore        # 已有：课程 PDF 等原始资料不上传
└── PRD.md / research.md / TECH_DESIGN.md   # 项目文档（不参与网站渲染）
```

**要点：** 内容（notes/）和骨架（index.html、_sidebar.md）分开；文档和站点分开——以后换渲染方案只动骨架，笔记一根汗毛都不碰。

## 4. 数据对象及字段（对应 PRD 第 5 节）

### 对象 1：笔记（Note）——核心数据对象，实体是 `notes/` 下的 `.md` 文件

| 字段 | 类型 | 存放在哪 | 用在哪 |
|---|---|---|---|
| `title` | 文本 | `_sidebar.md` 条目文字 | 目录条目、搜索结果显示 |
| `chapter` | 文本 | `_sidebar.md` 的分组标题 | 目录按章分组、搜索结果标注位置 |
| `category` | 文本 | `_sidebar.md` 第一层分组 | 目录第一层，本期固定"海事信息化课程"，为扩栏目预留 |
| `file` | 路径 | `_sidebar.md` 链接目标 | docsify 按此路径加载正文 |
| `updated` | 日期 | 每篇笔记开头一行标注 | 目录条目的辅助信息 |
| `content` | Markdown 正文 | `.md` 文件本身 | 渲染成文章、搜索取词的原料 |

### 对象 2：搜索索引（SearchIndex）——运行时生成，不存在任何文件里

| 字段 | 说明 |
|---|---|
| 生成方式 | docsify 搜索插件在**访客浏览器里**把全部 `.md` 分词建索引 |
| 生命周期 | 只存在于浏览器内存：每次打开页面现场重建，刷新即重来，**不占仓库、不占服务器** |
| 产出 | 搜索结果 = `title` + `chapter` + 匹配片段 + 跳转链接 |

### 对象 3：站点配置（Config）——写在 `index.html` 的常量

| 字段 | 说明 |
|---|---|
| `name` | 站名，显示在侧边栏顶部 |
| `basePath` | 内容根路径，本期就是仓库根目录 |
| `search` 配置 | 搜索插件开关与提示文案 |

## 5. API 列表

**如实说明：本项目没有后端，因此没有自定义 API。** 这不是缺失，而是第 1 节"纯静态"判断的直接结果——所有"接口"就是浏览器向 GitHub Pages 请求静态文件的 HTTP GET：

| # | 请求 | 触发时机 | 返回 | 失败时 |
|---|---|---|---|---|
| 1 | `GET /` | 打开网站 | `index.html`（含 docsify 脚本与配置） | 白屏 → 见第 7 节 E1 |
| 2 | `GET /_sidebar.md` | 每次进入/刷新 | 目录元数据 | 侧边栏空白 → E2 |
| 3 | `GET /notes/chapter-x/xxx.md` | 点击目录条目 | 笔记正文（Markdown） | 正文区显示 404 → E3 |
| 4 | `GET /_404.md` | 请求了不存在的路径 | 友好提示页 | 用默认 404 兜底 |

将来如果做登录、评论（本期不做清单第 1、3 项），才会真正需要 API 层和后端——那是"换项目级方案"的信号，不是加几个接口的事。

## 6. 数据流

```mermaid
flowchart LR
    A["📝 本地 Markdown 笔记<br/>(我的电脑, 数据的源头)"] -->|"git push"| B["🗄️ GitHub 仓库<br/>(兼任数据库, 存放笔记+元数据)"]
    B -->|"仓库开启 Pages 后自动发布"| C["🌐 GitHub Pages<br/>(分发, 免费托管)"]
    C -->|"浏览器请求页面和 .md 文件"| D["🖥️ 访客浏览器<br/>(docsify 运行时: 渲染 Markdown + 本地搜索)"]
    D -->|"排版好的文章 / 搜索结果"| E["👀 屏幕显示<br/>(我自己 + 同学)"]
```

**分步看一遍（每步都有产物，方便排错时定位卡在哪步）：**

| 步 | 动作 | 产物/可观察结果 |
|---|---|---|
| 1 | 我在本地改 `.md` 或 `_sidebar.md` | 文件内容变化 |
| 2 | `git add` + `git commit` + `git push` | GitHub 仓库更新（commit 出现在网页上） |
| 3 | GitHub Pages 自动发布 | 几十秒后线上内容同步（无需手动部署） |
| 4 | 访客打开链接 | 浏览器拿到 index.html，docsify 启动 |
| 5 | docsify 拉 `_sidebar.md` 渲染目录 | 首页显示章节导航 |
| 6 | 点条目 → 拉对应 `.md` → 渲染正文 | 看到排版好的文章 |
| 7 | 输入关键词 → 插件在内存索引中查 | 显示匹配笔记列表 |

## 7. 错误处理（每个编号对应一种能想到的坏情况）

| # | 坏情况 | 现象 | 处理方案 | 阶段 |
|---|---|---|---|---|
| E1 | `index.html` 引用的 docsify 脚本（CDN）加载失败 | 白屏 | 优先选稳定的 CDN；备选：把脚本下载进仓库本地引用（记入待确认） | Day 6 建 |
| E2 | `_sidebar.md` 缺失或路径写错 | 侧边栏空白 | 提交前自检：对照第 3 节结构逐个核对文件名（**大小写也要对**，Windows 不区分但线上区分，这是新手最常踩的坑） | 日常习惯 |
| E3 | 点了目录但 `.md` 文件不存在 | 正文区 404 | 改 `_sidebar.md` 时同步检查链接和文件是否成对增删；提供 `_404.md` 友好页 | Day 6 建 |
| E4 | 搜索无结果 | 找不到关键词 | 用 docsify 搜索插件自带提示，对应 PRD F3"不报错不白屏"的验收 | 插件自带 |
| E5 | 网络慢/断 | 页面加载卡住 | 静态站天然可重试刷新；不做额外处理（不加"防抖重试"这类超出 MVP 的机制） | 接受现状 |
| E6 | GitHub Pages 没开启或发布失败 | 访问 404 | 按第 9 节部署清单逐项检查；GitHub 仓库 Settings→Pages→Actions 页看部署日志 | 上线当天 |

## 8. 环境变量

**如实说明：纯静态前端没有服务端环境变量。** 传统项目里 `.env` 存数据库密码、API 密钥那一套，本项目不存在——因为没有服务器去读它们。

本项目实际的"配置"就是写死在 `index.html` 里的常量（见第 4 节对象 3），版本管理跟着 git 走，不需要额外机制。

**安全红线（比环境变量更重要）：**

- **前端 = 公开**：`index.html` 里写的任何内容全世界都能看到，**永远不放密码、Token、个人隐私**
- Day 3 实战里生成过 GitHub PAT（个人访问令牌）——它只存在 Windows 凭据管理器里，**绝不写进任何仓库文件**（`.gitignore` 已有防护，每次提交前再瞄一眼）
- 若将来真需要密钥（如接 AI 问答），正确做法是引入后端或 Serverless 函数——那时再回到"要不要后端"的选型

## 9. 部署注意事项（GitHub Pages，Day 6/7 上线时照单执行）

| # | 步骤 | 检查点 |
|---|---|---|
| 1 | 仓库 Settings → Pages → Source 选 "Deploy from a branch"，分支 `main`、目录 `/ (root)` | 保存后 Pages 页显示发布中的地址 |
| 2 | 确认仓库根目录有 `.nojekyll` 空文件 | 没有它，`_sidebar.md` 这类下划线开头文件会被 GitHub 内置的 Jekyll 静默忽略，侧边栏直接空白——**必踩坑，提前写在这里** |
| 3 | 等 1~2 分钟，用无痕窗口打开 `https://eric007228.github.io/haishi-xinxihua/` | 能看到首页目录 |
| 4 | 手机浏览器开同一链接（PRD F4 验收） | 排版正常可读 |
| 5 | 把链接发给一位同学实测 | 对方无说明也能看完一篇笔记（PRD 成功指标 2） |

**日常部署 = 日常 push：** 方案 A 下没有"发布"这个独立动作，push 即部署，1~2 分钟生效。排错口诀：线上不对，先确认"push 上去了没 → Pages 部署完了没 → 浏览器缓存刷掉没（Ctrl+F5）"。

## 10. 迁移注意事项（触发条件：GitHub Pages 实测不可用）

**A → B（GitHub Pages 换 Vercel + docsify 保持不变）：** 仓库文件零改动，只做三件事——注册 Vercel 并导入本仓库 → 部署设置选"无框架、根目录、无构建命令" → 把新链接更新进 README。丢掉的只有旧链接（提前告诉同学新地址）。

**A/B → MkDocs（渲染方案升级）：** 影响中等——装 Python、新增 `mkdocs.yml` 与构建步骤、目录结构按 MkDocs 约定重排；`notes/` 里的正文照搬，改的是骨架不是内容。

**共同的铁律：** 无论怎么换周边，`notes/` 里的 Markdown 是资产本体，任何迁移都以"内容零丢失"为底线——动手前先 `git push` 留存档，必要时打 tag。

## 11. 方案变化时，哪些文件会受影响

| 如果将来要变…… | 受影响的地方 | 改动量 |
|---|---|---|
| 换托管平台（GitHub Pages → Vercel） | 只改平台后台设置 + README 里的链接；**仓库里的文件一个不用动** | 极小 |
| 换渲染方案（docsify → MkDocs） | 删 `index.html`，改用 MkDocs 配置文件和目录结构，需引入构建步骤 | 中 |
| 加一个新栏目（如"航海英语"） | 只改 `_sidebar.md` 加一层分组 + 建对应笔记文件夹 | 小 |
| 加登录 / 评论功能 | 从"纯静态"升级为"有后端"，托管方式、安全设计全要重来 | 大（本期不做清单第 1、3 项） |

**规律：** 选型时把"可替换的"（托管）和"核心的"（Markdown 内容本身）分开，将来换周边工具就不伤筋动骨。

## 12. 附：什么是技术栈（大白话科普）

**技术栈 = 造一个网站/App 要用的成套工具，一层叠一层像堆盘子。** 以开发一个普通网页/App 为例：

| 层 | 常用技术 | 干什么用 | 优势 | 劣势 |
|---|---|---|---|---|
| 前端三件套 | HTML / CSS / JavaScript | 结构 / 样式 / 交互 | 一切网页的地基，浏览器原生支持 | 原生写法繁琐，大项目难维护 |
| 前端框架 | Vue、React | 把页面拆成可复用组件，省重复劳动 | 效率高、生态大 | 有学习曲线，小项目用是杀鸡用牛刀 |
| 后端语言 | Node.js、Python、Java | 处理业务逻辑、接口 | 能做登录、支付等动态功能 | 要买/租服务器，要管安全 |
| 数据库 | MySQL、SQLite | 结构化存数据、快速查询 | 数据量大也不乱 | 要设计表结构、做备份 |
| 静态托管 | GitHub Pages、Vercel | 免费发布纯静态站 | 零成本、免运维 | 只能放"内容不变就没动态行为"的站 |
| 静态站点方案 | docsify、MkDocs | 把 Markdown 变网站 | 内容即文件，写完即上线 | 动态功能（登录等）做不了 |

**本项目的栈（一句话）：** GitHub Pages（托管）+ docsify（渲染与搜索）+ Markdown（内容）+ git（搬运）——全部零成本、零服务器，正好匹配"纯静态"的定位。

## 13. 待确认与今日不做

**待确认（不阻塞 Day 5，Day 6 动手时定）：**

1. docsify 脚本用哪个 CDN 源，还是直接下载进仓库本地引用（影响 E1 的稳定性）——Day 6 实测定
2. `_404.md` 的具体文案与样式——抄模板起步即可

**今日不做：**

- 不写代码（`index.html`、docsify 配置文件是 Day 6 的事）
- 不回头大改 PRD（发现矛盾只记在"待确认"）
- 不注册 Vercel（先用 GitHub Pages 实测，不行再按第 10 节迁移）
