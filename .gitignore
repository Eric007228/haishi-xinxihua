# ============================================
# 不能公开的文件（黑名单：Git 会自动无视这些文件）
# ============================================

# --- 课程资料 PDF：只留在本机，不上传 ---
*.pdf

# --- 敏感信息文件（以后如果放了 API 密钥、账号密码，也不会被上传）---
.env
.env.*
*.key
*.pem
*secret*
*password*
*token*
config.local.*
credentials.*

# --- 系统临时文件 ---
Thumbs.db
desktop.ini
~$*
*.tmp

# --- 编辑器/工具的缓存 ---
.vscode/
.idea/
node_modules/
.workbuddy/
